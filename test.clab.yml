name: switchmap-test

topology:
  nodes:
    switch01:
      kind: linux
      image: alpine:latest
      exec:
        - apk add net-snmp net-snmp-tools
        - echo "rocommunity public" > /etc/snmp/snmpd.conf
        - snmpd -f &
    
    switch02:
      kind: linux  
      image: alpine:latest
      exec:
        - apk add net-snmp net-snmp-tools
        - echo "rocommunity public" > /etc/snmp/snmpd.conf
        - snmpd -f &
